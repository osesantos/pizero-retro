# =============================================================================
# Anemoia-PiZero - /boot/config.txt additions
# =============================================================================
# Copy the relevant sections below into the END of your /boot/config.txt on
# the RetroPie SD card, then reboot.
#
# Your full /boot/config.txt is at the root of the FAT32 boot partition and
# can be edited from any PC before first boot.
# =============================================================================

# -----------------------------------------------------------------------------
# Enable SPI bus (required for the ILI9341 display)
# -----------------------------------------------------------------------------
dtparam=spi=on

# -----------------------------------------------------------------------------
# Force HDMI output at 320x240.
# fbcp-ili9341 reads /dev/fb0 and pushes it directly to the ILI9341 over SPI.
# These settings are required even if no HDMI monitor is connected.
# -----------------------------------------------------------------------------
hdmi_force_hotplug=1
hdmi_group=2
hdmi_mode=87
hdmi_cvt=320 240 60 1 0 0 0

# -----------------------------------------------------------------------------
# Optional: disable overscan if you see black borders on an HDMI monitor
# -----------------------------------------------------------------------------
# disable_overscan=1
